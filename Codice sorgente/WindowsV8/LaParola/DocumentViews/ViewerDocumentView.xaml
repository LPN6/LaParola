<UserControl x:Class="LaParola.DocumentViews.ViewerDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource AppForegroundBrush}">
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top" Background="{DynamicResource ControlBackgroundBrush}">
            <ToolBar>
                <Button Content="{DynamicResource MenuOpen}" Click="Open_Click" />
            </ToolBar>
        </ToolBarTray>

        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="{DynamicResource ControlBackgroundBrush}" BorderBrush="{DynamicResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="12" Padding="12">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Left" FontWeight="SemiBold" Text="{DynamicResource FileLabel}"/>
                    <TextBlock DockPanel.Dock="Left" Margin="10,0,0,0" Text="{Binding CurrentFileDisplay}"/>
                </DockPanel>
            </Border>

            <RichTextBox x:Name="Viewer" Grid.Row="1" Margin="0,12,0,0" IsReadOnly="True" IsDocumentEnabled="True" VerticalScrollBarVisibility="Auto"/>
        </Grid>
    </DockPanel>
</UserControl>
