<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Color x:Key="AppBackground">#FF0B1220</Color>
    <Color x:Key="ControlBackground">#FF111A2B</Color>
    <Color x:Key="AppForeground">#FFE5E7EB</Color>
    <Color x:Key="SubtleBorder">#FF22314A</Color>
    <Color x:Key="Accent">#FF60A5FA</Color>
    <Color x:Key="ControlForeground">#FFE5E7EB</Color>
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource AppBackground}"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF2B2B2B"/>
    <SolidColorBrush x:Key="ControlBackgroundBrush" Color="{StaticResource ControlBackground}"/>
    <SolidColorBrush x:Key="ControlForegroundBrush" Color="{StaticResource ControlForeground}"/>
    <SolidColorBrush x:Key="AppForegroundBrush" Color="{StaticResource AppForeground}"/>
    <SolidColorBrush x:Key="SubtleBorderBrush" Color="{StaticResource SubtleBorder}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource Accent}"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuBrushKey}" Color="#FF2D2D30"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF2D2D30"/>
    <SolidColorBrush x:Key="{x:Static SystemColors.MenuTextBrushKey}" Color="White"/>

    <SolidColorBrush x:Key="ComboBox.Hover.Background" Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="ComboBox.Open.Background" Color="#FF404040"/>

    <SolidColorBrush x:Key="MenuBgBrush" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="MenuFgBrush" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="MenuHoverBgBrush" Color="#FF2D2D2D"/>
    <SolidColorBrush x:Key="MenuBorderBrush" Color="#FF3A3A3A"/>

    <SolidColorBrush x:Key="Menu.Popup.Background" Color="#FF1E1E1E"/>
    <SolidColorBrush x:Key="Menu.Popup.Foreground" Color="#FFF0F0F0"/>
    <SolidColorBrush x:Key="Menu.Popup.Border"     Color="#FF3A3A3A"/>
    <SolidColorBrush x:Key="Menu.Popup.Hover"      Color="#FF2A2A2A"/>
    <SolidColorBrush x:Key="Menu.Static.Background" Color="#FF1E1E1E"/>

    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource AppForegroundBrush}"/>
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AppForegroundBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="6,3"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                            Padding="{TemplateBinding Padding}"
                        BorderThickness="{TemplateBinding BorderThickness}">

                        <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"
                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>

                    <ControlTemplate.Triggers>

                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border"
                                Property="Background"
                                Value="{StaticResource ComboBox.Hover.Background}"/>
                        </Trigger>

                        <!-- Pressed -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border"
                                Property="Background"
                                Value="{StaticResource ComboBox.Open.Background}"/>
                        </Trigger>

                        <!-- Disabled -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border"
                                Property="Opacity"
                                Value="0.5"/>
                            <Setter Property="Foreground"
                                Value="#FF777777"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="ComboBox">
        <!--  Property="Foreground" Value="{StaticResource AppForegroundBrush}"/> TODO2 ComboBox background not follow theme settings, so need to keep text dark-->
        <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SubtleBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{StaticResource AppForegroundBrush}"/>
        <Setter Property="Background" Value="Transparent"/>

        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource ComboBox.Hover.Background}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource ComboBox.Open.Background}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ContextMenu surface (also influences submenu popup visuals) -->
    <Style TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="{StaticResource MenuBgBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource MenuFgBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MenuBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- MenuItem base -->
    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource MenuFgBrush}"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="BorderBrush" Value="Transparent"/>

        <!-- Hover/selection colors -->
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource MenuHoverBgBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="#FF777777"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{StaticResource AppForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{StaticResource AppForegroundBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <BulletDecorator Background="Transparent" SnapsToDevicePixels="True">
                        <BulletDecorator.Bullet>
                            <Grid Width="16" Height="16">
                                <Border x:Name="Box"
                                    CornerRadius="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" />

                                <!-- La SPUNTA: usa Foreground -->
                                <Path x:Name="Tick"
                                  Data="M 2 8 L 6.2 12.2 L 14 3.5"
                                  Stroke="{TemplateBinding Foreground}"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Visibility="Collapsed"/>
                            </Grid>
                        </BulletDecorator.Bullet>

                        <ContentPresenter Margin="6,0,0,0"
                                      VerticalAlignment="Center"
                                      RecognizesAccessKey="True"/>
                    </BulletDecorator>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Tick" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="#FF3A3A3A"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Tick" Property="Opacity" Value="0.4"/>
                            <Setter TargetName="Box" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
