<Window x:Class="LaParola.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ad="https://github.com/Dirkster99/AvalonDock"
        xmlns:local="clr-namespace:LaParola"
        xmlns:views="clr-namespace:LaParola.ToolViews"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="{DynamicResource AppTitle}" Width="1200" Height="780"
        Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource AppForegroundBrush}"
        Icon="/appicon.ico" WindowState="Maximized">
    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{x:Static local:MainWindow.NuovoCommand}" />
        <KeyBinding Key="O" Modifiers="Ctrl" Command="{x:Static local:MainWindow.ApriCommand}" />
        <KeyBinding Key="F4" Modifiers="Ctrl" Command="{x:Static local:MainWindow.ChiudiCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl" Command="{x:Static local:MainWindow.SalvaCommand}" />
        <KeyBinding Key="S" Modifiers="Ctrl+Shift" Command="{x:Static local:MainWindow.SalvaCommand}" />
        <KeyBinding Key="D" Modifiers="Ctrl" Command="{x:Static local:MainWindow.FontCommand}" />
        <KeyBinding Key="F3" Command="{x:Static local:MainWindow.FindNextCommand}" />
        <KeyBinding Key="F4" Command="{x:Static local:MainWindow.ReplaceNextCommand}" />
        <KeyBinding Key="F4" Modifiers="Alt" Command="{x:Static local:MainWindow.EsciCommand}" />
        <KeyBinding Key="L" Modifiers="Ctrl" Command="{x:Static local:MainWindow.LibraryCommand}" />
        <KeyBinding Key="F5" Command="{x:Static local:MainWindow.SearchCommand}" />
        <KeyBinding Key="F6" Command="{x:Static local:MainWindow.MostraCommand}" />
        <KeyBinding Key="F10" Command="{x:Static local:MainWindow.ConverterCommand}" />
        <KeyBinding Key="O" Modifiers="Ctrl+Shift" Command="{x:Static local:MainWindow.OptionsCommand}" />
    </Window.InputBindings>
    <Window.CommandBindings>
        <!-- This hooks up the Delete command to the built-in text editor logic -->
        <CommandBinding Command="ApplicationCommands.Delete" Executed="DeleteText_Executed" CanExecute="DeleteText_CanExecute"/>
        <CommandBinding Command="{x:Static local:MainWindow.NuovoCommand}" Executed="NuovoEditor_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.ApriCommand}" Executed="ApriEditor_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.ChiudiCommand}" Executed="ChiudiEditor_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.SalvaCommand}" Executed="SalvaEditor_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.SalvaComeCommand}" Executed="SalvaCome_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.FontCommand}" Executed="Font_Executed" />
        <CommandBinding Command="ApplicationCommands.Find" CanExecute="Find_CanExecute" Executed="Find_Executed" />
        <CommandBinding Command="ApplicationCommands.Replace" CanExecute="Replace_CanExecute" Executed="Replace_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.EsciCommand}" Executed="Esci_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.FindNextCommand}" Executed="FindNext_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.ReplaceNextCommand}" Executed="ReplaceNext_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.LibraryCommand}" Executed="Library_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.SearchCommand}" Executed="Search_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.MostraCommand}" Executed="Mostra_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.ConverterCommand}" Executed="Converter_Executed" />
        <CommandBinding Command="{x:Static local:MainWindow.OptionsCommand}" Executed="Options_Executed" />
    </Window.CommandBindings>
    <Grid x:Name="LayoutRoot">
        <DockPanel>
            <Menu DockPanel.Dock="Top" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource AppForegroundBrush}">
                <MenuItem Header="{DynamicResource MenuFile}">
                    <MenuItem Header="{DynamicResource MenuNewEditor}" Command="{x:Static local:MainWindow.NuovoCommand}" InputGestureText="Ctrl+N">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="FilePlus"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuApri" Header="{DynamicResource MenuApri}" Command="{x:Static local:MainWindow.ApriCommand}" InputGestureText="Ctrl+O">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="FolderOpen"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuChiudi" Header="{DynamicResource MenuChiudi}" Command="{x:Static local:MainWindow.ChiudiCommand}" InputGestureText="Ctrl+F4">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Close"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem x:Name="MenuSalva" Header="{DynamicResource MenuSalva}" IsEnabled="False" Command="{x:Static local:MainWindow.SalvaCommand}" InputGestureText="Ctrl+S">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="ContentSave"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuSalvaCome" Header="{DynamicResource MenuSalvaCome}" IsEnabled="False" Command="{x:Static local:MainWindow.SalvaComeCommand}" InputGestureText="Ctrl+Shift+S">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="ContentSaveEdit"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="{DynamicResource MenuEsci}" Command="{x:Static local:MainWindow.EsciCommand}" InputGestureText="Alt+F4"/>
                </MenuItem>
                <MenuItem Header="{DynamicResource MenuModifica}">
                    <MenuItem Header="{DynamicResource MenuAnnulla}" Command="ApplicationCommands.Undo">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Undo"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuRipristina}" Command="ApplicationCommands.Redo">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Redo"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="{DynamicResource MenuTaglia}" Command="ApplicationCommands.Cut">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="ContentCut"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuCopia}" Command="ApplicationCommands.Copy">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="ContentCopy"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuIncolla}" Command="ApplicationCommands.Paste">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="ContentPaste"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuElimina}" Command="ApplicationCommands.Delete">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Delete"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuSelezionaTutto" Header="{DynamicResource MenuSelezionaTutto}" Command="ApplicationCommands.SelectAll">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="SelectAll"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem x:Name="MenuCarattere" Header="{DynamicResource MenuCarattere}" Command="{x:Static local:MainWindow.FontCommand}" InputGestureText="Ctrl+D">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="FormatFont"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuAlign" Header="{DynamicResource MenuAllineamento}" SubmenuOpened="Allineamento_SubmenuOpened">
                            <MenuItem x:Name="MenuAlignLeft" Header="{DynamicResource MenuAllineamentoSinistra}" Command="EditingCommands.AlignLeft" IsCheckable="True">
                                <MenuItem.Icon>
                                    <iconPacks:PackIconMaterial Kind="AlignHorizontalLeft"/>
                                </MenuItem.Icon>
                            </MenuItem>
                            <MenuItem x:Name="MenuAlignCenter" Header="{DynamicResource MenuAllineamentoCentro}" Command="EditingCommands.AlignCenter" IsCheckable="True">
                            <MenuItem.Icon>
                                <iconPacks:PackIconMaterial Kind="AlignHorizontalCenter"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="MenuAlignRight" Header="{DynamicResource MenuAllineamentoDestra}" Command="EditingCommands.AlignRight" IsCheckable="True">
                            <MenuItem.Icon>
                                <iconPacks:PackIconMaterial Kind="AlignHorizontalRight"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="MenuAlignJustify" Header="{DynamicResource MenuAllineamentoGiustificato}" Command="EditingCommands.AlignJustify" IsCheckable="True">
                            <MenuItem.Icon>
                                <iconPacks:PackIconMaterial Kind="AlignHorizontalDistribute"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </MenuItem>
                    <Separator/>
                    <MenuItem x:Name="MenuTrova" Header="{DynamicResource MenuTrova}" Command="ApplicationCommands.Find">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Magnify"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuTrovaProssima" Header="{DynamicResource MenuTrovaProssima}" Command="{x:Static local:MainWindow.FindNextCommand}" InputGestureText="F3">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="MagnifyPlus"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuSostituisci" Header="{DynamicResource MenuSostituisci}" Command="ApplicationCommands.Replace">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="FindReplace"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuSostituisciProssima" Header="{DynamicResource MenuSostituisciProssima}" Command="{x:Static local:MainWindow.ReplaceNextCommand}" InputGestureText="F4">
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="{DynamicResource MenuVisualizza}">
                    <MenuItem x:Name="MenuVisualizzaBibbia" Header="{DynamicResource MenuVisualizzaBibbia}" Click="MenuVisualizzaBibbia_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="BookCross"/>
                        </MenuItem.Icon>
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding}"/>
                                <EventSetter Event="Click" Handler="SubMenuBibbia_Click"/>
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                    <MenuItem x:Name="MenuVisualizzaCommentario" Header="{DynamicResource MenuVisualizzaCommentario}" Click="MenuVisualizzaCommentario_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="BookOpenPageVariant"/>
                        </MenuItem.Icon>
                        <MenuItem.ItemContainerStyle>
                            <Style TargetType="MenuItem">
                                <Setter Property="Header" Value="{Binding}"/>
                                <EventSetter Event="Click" Handler="SubMenuCommentario_Click"/>
                            </Style>
                        </MenuItem.ItemContainerStyle>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="{DynamicResource MenuStrumenti}">
                    <MenuItem x:Name="MenuBiblioteca" Header="{DynamicResource MenuBiblioteca}" Command="{x:Static local:MainWindow.LibraryCommand}" InputGestureText="Ctrl+L">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Bookshelf"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuRicerca}" Command="{x:Static local:MainWindow.SearchCommand}" InputGestureText="F5">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Magnify"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuTextGenerator}" Command="{x:Static local:MainWindow.MostraCommand}" InputGestureText="F6">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="BookOpenPageVariant"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuConverter}" Command="{x:Static local:MainWindow.ConverterCommand}" InputGestureText="F10">

                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Ruler"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuOptions}" Command="{x:Static local:MainWindow.OptionsCommand}" InputGestureText="Ctrl+Shift+O">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Cog"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="{DynamicResource MenuAiuto}">
                    <MenuItem Header="{DynamicResource MenuAbout}" Click="About_Click"/>
                </MenuItem>
            </Menu>

            <ad:DockingManager x:Name="Dock" AllowMixedOrientation="True" Background="{DynamicResource AppBackgroundBrush}">
                <ad:LayoutRoot>
                    <ad:LayoutPanel Orientation="Horizontal">

                        <ad:LayoutAnchorablePaneGroup DockWidth="320">
                            <ad:LayoutAnchorablePane>
                            </ad:LayoutAnchorablePane>
                        </ad:LayoutAnchorablePaneGroup>

                        <ad:LayoutDocumentPaneGroup>
                            <ad:LayoutDocumentPane x:Name="DocumentPane" />
                        </ad:LayoutDocumentPaneGroup>

                    </ad:LayoutPanel>
                </ad:LayoutRoot>
            </ad:DockingManager>
        </DockPanel>

        <Grid x:Name="LoadingOverlay"
              Visibility="Collapsed"
              Panel.ZIndex="9999"
              Background="#80000000"
              Focusable="True"
              IsHitTestVisible="True">

            <Border Background="#FF1E1E1E"
                    CornerRadius="12"
                    Padding="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="260">
                <StackPanel>
                    <TextBlock x:Name="LoadingText"
                               Text="Caricamento…"
                               Foreground="White"
                               FontSize="14"
                               Margin="0,0,0,10"
                               HorizontalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Height="10"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
