<Window x:Class="LaParola.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ad="https://github.com/Dirkster99/AvalonDock"
        xmlns:views="clr-namespace:LaParola.ToolViews"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="{DynamicResource AppTitle}" Width="1200" Height="780"
        Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource AppForegroundBrush}"
        Icon="/appicon.ico" WindowState="Maximized">
    <Grid x:Name="LayoutRoot">
        <DockPanel>
            <Menu DockPanel.Dock="Top" Background="{DynamicResource ControlBackgroundBrush}" Foreground="{DynamicResource AppForegroundBrush}">
                <MenuItem Header="{DynamicResource MenuFile}">
                    <MenuItem Header="{DynamicResource MenuNewEditor}" Click="NewEditor_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="FilePlus"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuApri}" Click="ApriEditor_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="FolderOpen"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuNewViewer}" Click="NewViewer_Click" Visibility="Collapsed"/>
                    <MenuItem x:Name="MenuChiudi" Header="{DynamicResource MenuChiudi}" IsEnabled="False" Click="Chiudi_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Close"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem x:Name="MenuSalva" Header="{DynamicResource MenuSalva}" IsEnabled="False" Click="Salva_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="ContentSave"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem x:Name="MenuSalvaCome" Header="{DynamicResource MenuSalvaCome}" IsEnabled="False" Click="SalvaCome_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="ContentSaveEdit"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator/>
                    <MenuItem Header="{DynamicResource MenuEsci}" Click="Exit_Click"/>
                </MenuItem>
                <MenuItem Header="{DynamicResource MenuModifica}">
                    <MenuItem Header="{DynamicResource MenuAllineamento}" SubmenuOpened="Allineamento_SubmenuOpened">
                        <MenuItem x:Name="MiAlignLeft" Header="{DynamicResource MenuAllineamentoSinistra}" Command="EditingCommands.AlignLeft" IsCheckable="True">
                            <MenuItem.Icon>
                                <iconPacks:PackIconMaterial Kind="AlignHorizontalLeft"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="MiAlignCenter" Header="{DynamicResource MenuAllineamentoCentro}" Command="EditingCommands.AlignCenter" IsCheckable="True">
                            <MenuItem.Icon>
                                <iconPacks:PackIconMaterial Kind="AlignHorizontalCenter"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="MiAlignRight" Header="{DynamicResource MenuAllineamentoDestra}" Command="EditingCommands.AlignRight" IsCheckable="True">
                            <MenuItem.Icon>
                                <iconPacks:PackIconMaterial Kind="AlignHorizontalRight"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem x:Name="MiAlignJustify" Header="{DynamicResource MenuAllineamentoGiustificato}" Command="EditingCommands.AlignJustify" IsCheckable="True">
                            <MenuItem.Icon>
                                <iconPacks:PackIconMaterial Kind="AlignHorizontalDistribute"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="{DynamicResource MenuStrumenti}">
                    <MenuItem Header="{DynamicResource MenuTextGenerator}" Click="ShowTextGenerator_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="BookOpenPageVariant"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuConverter}" Click="ShowConverter_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Ruler"/>
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="{DynamicResource MenuOptions}" Click="ShowOptions_Click">
                        <MenuItem.Icon>
                            <iconPacks:PackIconMaterial Kind="Cog"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
                <MenuItem Header="{DynamicResource MenuAiuto}">
                    <MenuItem Header="{DynamicResource MenuAbout}" Click="About_Click"/>
                </MenuItem>
            </Menu>

            <ad:DockingManager x:Name="Dock" AllowMixedOrientation="True" Background="{DynamicResource AppBackgroundBrush}">
                <ad:LayoutRoot>
                    <ad:LayoutPanel Orientation="Horizontal">

                        <ad:LayoutAnchorablePaneGroup DockWidth="320">
                            <ad:LayoutAnchorablePane>
                                <ad:LayoutAnchorable x:Name="ConverterToolAnchorable" ContentId="tool.converter" Title="{DynamicResource MisureTitolo}" CanClose="False" CanHide="True">
                                    <views:ConverterToolView />
                                </ad:LayoutAnchorable>
                                <ad:LayoutAnchorable x:Name="OptionsToolAnchorable" ContentId="tool.options" Title="{DynamicResource OpzioniTitolo}" CanClose="False" CanHide="True">
                                    <views:OptionsToolView />
                                </ad:LayoutAnchorable>
                            </ad:LayoutAnchorablePane>
                        </ad:LayoutAnchorablePaneGroup>

                        <ad:LayoutDocumentPaneGroup>
                            <ad:LayoutDocumentPane x:Name="DocumentPane" />
                        </ad:LayoutDocumentPaneGroup>

                    </ad:LayoutPanel>
                </ad:LayoutRoot>
            </ad:DockingManager>
        </DockPanel>

        <Grid x:Name="LoadingOverlay"
              Visibility="Collapsed"
              Panel.ZIndex="9999"
              Background="#80000000"
              Focusable="True"
              IsHitTestVisible="True">

            <Border Background="#FF1E1E1E"
                    CornerRadius="12"
                    Padding="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="260">
                <StackPanel>
                    <TextBlock x:Name="LoadingText"
                               Text="Caricamento…"
                               Foreground="White"
                               FontSize="14"
                               Margin="0,0,0,10"
                               HorizontalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Height="10"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
